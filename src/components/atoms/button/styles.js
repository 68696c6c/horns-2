import styled from '@emotion/styled'

import { anchor } from '../../hadrons/anchor'

// eslint-disable-next-line import/prefer-default-export
export const Button = styled.button(...anchor.styles('button'))
