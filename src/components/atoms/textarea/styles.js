import styled from '@emotion/styled'

import { control } from '../_base/controllable'

// eslint-disable-next-line import/prefer-default-export
export const Textarea = styled.textarea(...control.styles(false))
